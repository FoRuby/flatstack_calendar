$('#calendar').fullCalendar('removeEvents', [<%= @event.id %>]);
$('#calendar').fullCalendar(
  'renderEvent',
  $.parseJSON("<%=j render @event, action: :show, format: :json %>"), true
);
$('.alert').alert();
